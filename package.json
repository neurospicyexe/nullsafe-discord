{
  "name": "nullsafe-discord",
  "private": true,
  "workspaces": ["packages/*", "bots/*"],
  "scripts": {
    "build": "npm run build -w packages/shared && npm run build --workspaces --if-present",
    "test": "npm test --workspaces --if-present"
  }
}
